name: Java CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:   
    - uses: actions/checkout@v1
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 1.11
    - name: Build with Maven
      env: 
        SONAR_URL: ${{ secrets.SONAR_CLOUD_URL }}
        SONAR_TOKEN: ${{ secrets.SONAR_CLOUD_TOKEN }}    
      run: mvn clean package sonar:sonar -Dsonar.host.url=$SONAR_CLOUD_URL -Dsonar.login=$SONAR_CLOUD_TOKEN --file pom.xml
